:root {
    --bg: #0f172a;
    --text: #f1f5f9;
    --accent: #38bdf8;
    --glass: rgba(255, 255, 255, 0.03);
    --border: rgba(255, 255, 255, 0.1);
}

body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg);
    color: var(--text);
    margin: 0;
    line-height: 1.6;
}

.glass-background {
    position: fixed;
    top: -100px;
    right: -100px;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, #38bdf822 0%, transparent 70%);
    z-index: -1;
}

nav {
    display: flex;
    justify-content: space-between;
    padding: 2rem 10%;
}

.logo { font-weight: 700; color: var(--accent); }

nav ul { display: flex; list-style: none; gap: 2rem; }

nav a { text-decoration: none; color: var(--text); font-weight: 300; }

header {
    height: 70vh;
    display: flex;
    align-items: center;
    padding: 0 10%;
}

.hero h1 { font-size: 3.5rem; margin-bottom: 1rem; }

.gradient-text {
    .gradient-text {
    background: linear-gradient(90deg, #38bdf8, #818cf8);
    /* Standard property for modern browsers */
    background-clip: text; 
    /* Webkit prefix for Chrome, Safari, and older versions of Edge/Firefox */
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    /* Fallback color for very old browsers that don't support text gradients */
    color: #38bdf8; 
}
}

.hero p { font-size: 1.2rem; color: #94a3b8; max-width: 500px; margin-bottom: 2rem; }

.btn {
    padding: 0.8rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    background: var(--accent);
    color: var(--bg);
}

.project-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    margin-top: 20px;
}

.project-card {
    background: var(--glass);
    border: 1px solid var(--border);
    padding: 2rem;
    border-radius: 12px;
}

.tech-tag {
    font-size: 0.7rem;
    color: var(--accent);
    background: rgba(56, 189, 248, 0.1);
    padding: 4px 8px;
    border-radius: 4px;
}